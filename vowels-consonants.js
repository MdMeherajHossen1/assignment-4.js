let s = ['a', 'w', 'e', 'q', 'i', 'm', 'o', 'k', 'u', 's', 'g'];

function checkVowelsConsonants (s){
    // const vowels = [];
    // const consona
    for (const number of s){
        if((number === 'a') || (number == 'e') || (number=== 'i') || (number === 'o') || (number === 'u')){
            console.log(s);
        }
        else{
            console.log(number);
        }
    }
    // console.log(vowels);
    // console.log(consonants);
}